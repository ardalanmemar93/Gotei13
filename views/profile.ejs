<%- include('./partials/header') %>
<body class="profile-body">
  <div>
  <div class="container">
    <h1 class="mt-5 white user-profile">User Profile</h1>
    <h2 class="mb-4 white poiret">Welcome, <%= user.name %>!</h2>
    <h3 class="white poiret">Your Characters:</h3>

    <div class="row">
      <% characters.forEach(function(character) { %>
      <div class="col-md-6">
        <div class="card mb-4">
          <div class="card-body bg-dark">
            <h5 class="card-title poiret-card" style="color: white;"><strong>Name:</strong> <%= character.name %></h5>
            <p class="card-text poiret-sub"><strong>Race:</strong> <%= character.race %></p>
            <p class="card-text poiret-sub"><strong>Background:</strong> <%= character.background %></p>
            <p class="card-text poiret-sub"><strong>Class:</strong> <%= character.dndClass %></p>
            <p class="card-text poiret-sub"><strong>Equipment:</strong> <%= character.equipment.join(', ') %></p>

            <a href="/characters/update/<%= character._id %>" class="btn btn-outline-primary mr-2">Update</a>
            <form method="POST" action="/characters/<%= character._id %>/publish">
              <button type="submit" class="btn btn-outline-success mr-2">Publish</button>
            </form>
            <form method="POST" action="/characters/<%= character._id %>?_method=DELETE">
              <button type="submit" class="btn btn-outline-danger">Delete !</button>
            </form>
          </div>
        </div>
      </div>
      <% }); %>
    </div>
  </div>
  </div>
</body>
</html>
